---
openapi: 'PUT /v2/services/{uuid}'
---

This endpoint updates an existing service monitor.

### Path Parameters

<ParamField path="uuid" type="string" required>
  The universally unique identifier of the service monitor.
</ParamField>

### Body

You can send the same parameters as when creating a service monitor to update its values. 

